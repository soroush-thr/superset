import React from 'react'

// Placeholder. Full implementation (equipment profiles, targets table,
// export/import, provenance) is section 9.5, Phase 9.
export default function Settings() {
  return (
    <div>
      <span className="sx-eyebrow">Settings</span>
      <h2>Equipment, targets, export / import</h2>
      <p style={{ color: 'var(--text-dim)' }}>Settings land in Phase 9.</p>
    </div>
  )
}
