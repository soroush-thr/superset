import React from 'react'

// Placeholder. Full implementation (body maps, the sub-group ledger, gap
// panel, credit mode toggle) is section 9.4, Phase 8.
export default function Coverage() {
  return (
    <div>
      <span className="sx-eyebrow">Coverage</span>
      <h2>Build a routine to see where your volume lands.</h2>
      <p style={{ color: 'var(--text-dim) ' }}>
        The ledger, gap suggestions, and credit mode toggle land in Phase 8.
      </p>
    </div>
  )
}
