import React from 'react'

// Placeholder. Full implementation (search, filters, virtualized list,
// equipment profile switch, detail drawer) is section 9.1, Phase 6.
export default function Library() {
  return (
    <div>
      <span className="sx-eyebrow">Library</span>
      <h2>876 exercises</h2>
      <p style={{ color: 'var(--text-dim)' }}>
        Search, filters, and the exercise list land in Phase 6.
      </p>
    </div>
  )
}
